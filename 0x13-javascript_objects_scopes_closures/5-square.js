#!/usr/bin/node
/**
 *quare class that inherits from rectangle class
 */
const Rectangle = require('./4-rectangle');

classquare extends Rectangle {
  constructor (size) {
    super(size, size);
  }
}

module.exports =quare;
